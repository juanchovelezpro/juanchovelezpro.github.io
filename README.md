# juanchovelezpro.github.io

React + Vite + TypeScript portfolio for GitHub Pages.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Deployment

GitHub Pages deployment is configured through GitHub Actions in `.github/workflows/deploy.yml`.
